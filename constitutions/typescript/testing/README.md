# TypeScript testing

The test runner, what coverage is required, and what a test looks like.

| Document | Covers |
|---|---|
| [Testing on the web](testing-web.md) | The Bun test runner, coverage, file layout, and component tests |
| [Testing React Native](testing-react-native.md) | What a React Native package does differently under Jest and RNTL |

The rule numbers run continuously across the two files: the web rules are 1–12 and the React
Native carve-out picks up at 13, so no number means two things.
