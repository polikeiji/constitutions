# TypeScript tooling

The runtime, package manager, compiler, formatter and bundler every TypeScript package runs
on.

| Document | Covers |
|---|---|
| [Tooling for the web](tooling-web.md) | Bun, the compiler, `tsconfig.json`, Biome, and bundling |
| [Tooling for React Native](tooling-react-native.md) | What a React Native package does differently under Expo and Metro |

The rule numbers run continuously across the two files: the web rules are 1–13 and the React
Native carve-out picks up at 14, so no number means two things.
